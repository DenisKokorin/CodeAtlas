services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: code-atlas-backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      - backend_data:/app/data

volumes:
  backend_data: